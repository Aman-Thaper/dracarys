# DRACARYS platform runtime dependencies
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.3
SQLAlchemy[asyncio]>=2.0.30
aiosqlite>=0.20
alembic>=1.13
httpx>=0.27
structlog>=24.1
tenacity>=8.4
# Optional LLM provider (planner works without it via the heuristic planner)
anthropic>=0.39
# Optional Postgres driver for production (DATABASE_URL=postgresql+asyncpg://...)
# asyncpg>=0.29
